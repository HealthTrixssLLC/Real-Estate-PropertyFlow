#!/usr/bin/env node
// Bare React Native app — there is no JS-only build for the Replit web
// preview. The user builds and runs this from Xcode directly. This stub
// keeps the artifact.toml `build` step a no-op so deploys don't fail.
console.log("[tour-mobile] bare React Native — no JS build needed for Replit");
process.exit(0);
